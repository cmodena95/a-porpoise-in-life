<h2><%= porpoise.name %></h2>
<p><%= porpoise.location %></p>
<% if porpoise.photos.attached? %>
    <% porpoise.photos.each do |photo| %>
        <%= cl_image_tag photo.key, height: 100, width: 200, crop: :fill, class: "colorful-shadow" %>
    <% end %>
<% end %>